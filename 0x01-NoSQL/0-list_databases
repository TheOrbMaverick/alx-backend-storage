#!/usr/bin/env mongo
// list all databases

print("Listing all databases:\n");

db.adminCommand('listDatabases').databases.forEach(function(db) {
    print(db.name + "\t" + db.sizeOnDisk / (1024 * 1024 * 1024) + "GB");
});
